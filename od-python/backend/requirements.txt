# ODRIV runtime requirements — only what the app actually imports.
# (The original scaffold pinned 130+ unused packages, including a wheel on a
#  private CDN that breaks installation; none of them are needed to run ODRIV.)
fastapi
uvicorn
motor
pymongo
mongomock-motor
python-dotenv
python-multipart
openpyxl
python-pptx
reportlab
matplotlib

# shared Access database (.accdb) read/write
access-parser
jaydebeapi
JPype1

# test suite (optional)
pytest
requests
